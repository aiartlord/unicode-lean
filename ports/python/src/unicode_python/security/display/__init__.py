"""Display-integrity detector family.

Detectors for hazards arising from the gap between a string's logical
codepoint order and how a bidi-aware renderer presents it — right-to-left
injection into a left-to-right-declared field being the first member.
"""
